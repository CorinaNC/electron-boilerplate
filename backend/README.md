## 1. Install dependencies:
```
# To do a clean install and also run tests
mvn clean install

# To do a clean install and skip tests
mvn clean install -DskipTests
```

## 2. Run:
```
mvn spring-boot:run
# or
./mvnw spring-boot:run
```